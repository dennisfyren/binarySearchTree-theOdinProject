## Description

The binary search tree have to be a balanced tree that stores data dynamically.

## Psuedo code

base = x

insert(value)
if (value < base)
if (left node total items !> right node total items)
put in left node
else put in right node
